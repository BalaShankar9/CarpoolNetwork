/**
 * Feature Flags
 *
 * Controls which features are active in the application.
 * For V1 private beta, deferred features are disabled.
 * Flip ENABLE_DEFERRED_FEATURES to true to restore Phase 2+ routes.
 */

export const ENABLE_DEFERRED_FEATURES = false;
