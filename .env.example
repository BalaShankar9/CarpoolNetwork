# Sentry Error Tracking
# Project: https://carpool-network-nd.sentry.io/projects/carpool-network/
# Leave empty in local dev — Sentry only initialises in staging/production
VITE_SENTRY_DSN=https://fa93d8fd5b328258ab67bd5fd5f7e560@o4510918472171520.ingest.us.sentry.io/4510918473678848

# Sentry Auth Token — for CI source map uploads only (NOT a VITE_ var, stays server-side)
# Generate at: https://carpool-network-nd.sentry.io/settings/auth-tokens/
# Add as GitHub secret: SENTRY_AUTH_TOKEN
SENTRY_AUTH_TOKEN=

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Google Maps API Key
# Get from: https://console.cloud.google.com/google/maps-apis
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Application Environment
# Options: development | staging | production
# Note: This is automatically set by netlify.toml for deployed environments
VITE_APP_ENV=development

# Beta Mode (set to true to restrict signups to allowlisted emails only)
VITE_BETA_MODE=false

# OTP signup behavior (defaults to false when VITE_BETA_MODE=true, otherwise true)
VITE_AUTH_ALLOW_OTP_SIGNUPS=true

# Admin Email (for beta allowlist management)
VITE_ADMIN_EMAIL=admin@carpoolnetwork.co.uk

# Email Configuration
# Disable email confirmation requirement in local development to prevent bounce rates
# Set to 'true' for local development, 'false' for production
VITE_SKIP_EMAIL_VERIFICATION=false

# Auth Feature Flags
# Control which authentication methods are enabled
VITE_AUTH_ALLOW_SIGNUPS=true
VITE_AUTH_ALLOW_PASSWORD_LOGIN=true
VITE_AUTH_ALLOW_EMAIL_OTP=true
VITE_AUTH_ALLOW_PHONE_OTP=true
VITE_AUTH_ALLOW_OAUTH=true
VITE_AUTH_REQUIRE_EMAIL_VERIFICATION=true
VITE_AUTH_REQUIRE_PROFILE_COMPLETION=true
VITE_AUTH_DEFAULT_COUNTRY=GB

# AI/Chatbot Configuration (optional - for help hub AI features)
# VITE_OPENAI_API_KEY=your-openai-key
# VITE_ANTHROPIC_API_KEY=your-anthropic-key

# ==========================================================
# STRIPE PAYMENTS (required for in-app payments & subscriptions)
# ==========================================================
# Frontend public key (safe to expose in browser)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Stripe Price IDs for membership plans (create in Stripe Dashboard → Products)
VITE_STRIPE_PLUS_MONTHLY_PRICE_ID=price_xxxxxxxxxxxxxxxx
VITE_STRIPE_PLUS_YEARLY_PRICE_ID=price_xxxxxxxxxxxxxxxx
VITE_STRIPE_PRO_MONTHLY_PRICE_ID=price_xxxxxxxxxxxxxxxx
VITE_STRIPE_PRO_YEARLY_PRICE_ID=price_xxxxxxxxxxxxxxxx

# Backend/Netlify functions only (NEVER expose these to the frontend)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# Supabase service-role key (Netlify functions only — NOT the anon key)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# App canonical URL (for Stripe success/cancel redirect URLs)
APP_URL=https://carpoolnetwork.co.uk
