# E2E Test Configuration
# Copy this file to .env.e2e and fill in actual test credentials

# Base URL for E2E tests (default: http://localhost:5173)
E2E_BASE_URL=http://localhost:5173

# IMPORTANT: Email Bounce Prevention
# To prevent Supabase email bounce warnings:
# 1. Create REAL email addresses for test accounts (use a real domain you control)
#    OR use a test email service like Ethereal.email or Mailtrap.io
# 2. In Supabase Dashboard > Authentication > Email Templates:
#    - Disable "Confirm email" for these specific test accounts
#    OR create a separate Supabase project for E2E testing
# 3. Never use fake/non-existent email addresses for testing

# Test Driver Account
# Create this account in Supabase and add to beta_allowlist
# MUST be a real, working email address to avoid bounce rates
E2E_DRIVER_EMAIL=e2e-driver@test.carpoolnetwork.co.uk
E2E_DRIVER_PASSWORD=TestDriver123!

# Test Passenger Account
# Create this account in Supabase and add to beta_allowlist
# MUST be a real, working email address to avoid bounce rates
E2E_PASSENGER_EMAIL=e2e-passenger@test.carpoolnetwork.co.uk
E2E_PASSENGER_PASSWORD=TestPassenger123!

# Admin Account (for beta management tests)
# MUST be a real, working email address to avoid bounce rates
E2E_ADMIN_EMAIL=admin@carpoolnetwork.co.uk
E2E_ADMIN_PASSWORD=AdminTest123!
